package gonumberio

import ite "github.com/Matej-Chmel/go-number-io/internal"

const (
	// Newline found when reading current element
	HasNewlineFlag = ite.HasNewline
	// Current element has at least one digit
	HasValueFlag = ite.HasValue
)
